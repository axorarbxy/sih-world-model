import {useState} from 'react'; export default function FileUpload({onResult}){const [busy,setBusy]=useState(false);const submit=async e=>{const f=e.target.files[0];if(!f)return;setBusy(true);try{const {uploadTraffic}=await import('../services/api');onResult(await uploadTraffic(f))}catch(err){alert(err.response?.data?.detail||err.message)}finally{setBusy(false)}};return <label className="upload">{busy?'ANALYSING WORLD STATE…':'UPLOAD PCAP OR CSV'}<input type="file" accept=".csv,.pcap,.pcapng" onChange={submit}/></label>}
